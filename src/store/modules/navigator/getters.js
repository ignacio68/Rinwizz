/*
export const pageStack = state => {
  return state.stack
}

export const options = state => {
  return state.options
}

export const index = state => {
  return state.index
}
*/

export default {
  pageStack: state => {
    return state.stack
  },

  options: state => {
    return state.options
  },

  index: state => {
    return state.index
  }
}
